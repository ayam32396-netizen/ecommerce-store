# 📝 دليل التعديلات - كيف تتحكم في متجرك

هذا الملف يشرح كل حاجة ممكن تحتاج تعدّلها في متجرك، سواء من لوحة التحكم أو من الكود مباشرة.

---

## 1️⃣ التعديل من لوحة التحكم (الطريقة السهلة - موصى بيها)

ادخل على: `http://localhost:3000/admin`

### ➕ إضافة منتج جديد
1. تبويب "المنتجات" → زر "+ إضافة منتج"
2. اكتب الاسم، الوصف، السعر، الكمية، واختر التصنيف
3. **لإضافة صورة**: دوس على حقل "صورة المنتج" واختار صورة من جهازك (jpg, png, webp, gif) - هتترفع تلقائياً وتظهر معاينة لها
4. لو غيّرت رأيك، دوس "🗑️ إزالة الصورة"
5. دوس "حفظ"

### ✏️ تعديل منتج (تغيير السعر، الاسم، الصورة...)
1. تبويب "المنتجات" → دوس "تعديل" جنب أي منتج
2. عدّل أي حقل تحب فيه (السعر، الاسم، الصورة، المخزون...)
3. دوس "حفظ"

### 🗑️ حذف منتج
- دوس "حذف" جنب المنتج المطلوب، وأكّد الحذف

### 📦 إدارة الطلبات
- تبويب "الطلبات" يعرض كل الطلبات
- تقدر تغيّر حالة الطلب من القائمة المنسدلة (قيد الانتظار → معالجة → شحن → تسليم)
- دوس "عرض" لمعرفة تفاصيل الطلب والمنتجات المطلوبة

> ✅ كل التعديلات دي بتتحفظ فورًا في قاعدة البيانات (`store.db`) وتظهر للعملاء مباشرة، من غير ما تحتاج تلمس كود.

---

## 2️⃣ التعديل المباشر على الكود (للمطورين)

لو عايز تغيّر حاجة مش موجودة في لوحة التحكم:

### تغيير اسم المتجر / الشعار
📄 الملف: `public/index.html`
```html
<h1 class="logo">👕 متجر الملابس</h1>
```
غيّر النص واللمز حسب اسم متجرك.

### تغيير الألوان العامة للمتجر
📄 الملف: `public/css/style.css` (أول الملف)
```css
:root {
  --primary: #6d28d9;   /* اللون الأساسي */
  --primary-dark: #5b21b6;
}
```

### تغيير الألوان في لوحة التحكم
📄 الملف: `public/admin/css/admin.css` (أول الملف - نفس المتغيرات)

### إضافة تصنيفات جديدة للمنتجات (غير رجالي/حريمي/أطفال...)
📄 الملف: `public/admin/dashboard.html` — دوّر على:
```html
<select id="productCategory">
  <option value="رجالي">رجالي</option>
  ...
</select>
```
ضيف `<option value="اسم التصنيف">اسم التصنيف</option>` جديد.

### تغيير بيانات دخول الأدمن الافتراضية
📄 الملف: `.env`
```
ADMIN_EMAIL=your-email@example.com
ADMIN_PASSWORD=YourNewPassword123
```
> ملاحظة: التعديل هنا بيشتغل بس أول مرة تشغّل فيها المشروع (أول ما ينشئ قاعدة البيانات). لو عايز تغيّر كلمة السر بعد كده، لازم تحذف حساب الأدمن القديم من قاعدة البيانات أو تعمل حساب جديد يدويًا.

### تغيير رقم البورت (المنفذ) اللي المشروع بيشتغل عليه
📄 الملف: `.env`
```
PORT=3000
```

---

## 3️⃣ فين المنتجات والطلبات متخزنة؟

كل حاجة متخزنة في ملف واحد اسمه **`store.db`** (قاعدة بيانات SQLite) في مجلد المشروع الرئيسي. الملف ده بينشئ نفسه أول ما تشغّل المشروع، وبيحتفظ بكل المنتجات والطلبات حتى لو قفلت السيرفر وشغّلته تاني.

⚠️ **لو عايز تبدأ من الصفر (تمسح كل المنتجات والطلبات)**: احذف ملف `store.db` وشغّل المشروع تاني، هيتعمل من جديد ببيانات تجريبية.

الصور اللي بترفعها من لوحة التحكم بتتخزن في مجلد: `public/uploads/`

---

## 4️⃣ نصائح عملية

- **قبل أي تعديل في الكود**: خد نسخة احتياطية من المشروع كامل (انسخ المجلد أو اعمل zip).
- **بعد أي تعديل في ملفات JS/HTML/CSS**: مفيش حاجة تحتاج تعمل build، بس اعمل refresh للصفحة في المتصفح.
- **بعد تعديل ملفات السيرفر** (`server.js`, `routes/*.js`, `config/db.js`): لازم توقف السيرفر (Ctrl+C) وتشغّله تاني (`npm start`).
- لو حصل خطأ بعد تعديل، ارجع للنسخة المحفوظة وقارن السطر اللي غيرته.
